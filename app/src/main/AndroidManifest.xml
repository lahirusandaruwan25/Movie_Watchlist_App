<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/welcome"
        android:label="@string/app_name"
        android:roundIcon="@drawable/welcome"
        android:supportsRtl="true"
        android:theme="@style/Theme.Movie_Watchlist_App">

        <activity android:name=".BrowseMoviesActivity"/>
        <activity android:name=".WatchlistActivity"/>
        <activity android:name=".AddMovieActivity"/>
        <activity android:name=".WatchedMoviesActivity"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".MainActivity"/>

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
